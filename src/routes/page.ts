const routes = {
  index: "/",
  login: "/login",
  register: "/register",
  error: {
    unauthorized: "/error/unauthorized",
    unauthenticated: "/error/unauthenticated",
  },
};

export default routes;
